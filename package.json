{
  "private": true,
  "packageManager": "yarn@3.2.2",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "scripts": {
    "clear": "rm -rf .turbo node_modules apps/app/node_modules apps/app/.expo apps/backend/node_modules apps/docs/node_modules apps/web/node_modules packages/ui-components/node_modules package-lock.json yarn.lock && yarn install",
    "pipeline": "turbo run lint build test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "synth": "turbo run synth"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "turbo": "^1.5.5"
  },
  "resolutions": {
    "metro": "0.73.1"
  }
}
